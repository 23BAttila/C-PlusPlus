#include <iostream>
#include <cmath>

int main(){

    double x = 3.1415;
    double y = 4.99;
    double z;
    //z = std::max(x,y);
    //z = std::min(x,y);
    //#include <cmath>
    //z = pow(x,y);
    //z = sqrt(y);
    //z = abs(-3);
    //z = round(x);
    //z = ceil(x); //round up
    //z=floor(y); //round down

    std::cout<<z;
}